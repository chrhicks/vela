# Imaging and processing

[Back to the field review](2026-09-14-first-light.md).

Dated source/evidence audit: September 15, 2026, PR #59 head `34a62367`. Planning only; no application changes, hardware access or new tests. Code links point to that revision. Local `data/` references belong to the original checkout and are Git-ignored. Existing boundary READMEs remain authoritative.

## Imaging priorities within the overall plan

| Priority | Work | Why / completion boundary |
|---|---|---|
| Useful next-session slice | Cooling status near capture, explicit supported controls | Chris missed that the cooler was off despite a near-setpoint sensor reading. Confirmed state is already partly available; do not build a readiness-policy engine. |
| Useful next-session slice | Display-only preview treatment, including retained previews | The green cast impairs ordinary inspection. New captures alone are insufficient because saved PNGs persist independently. Complete workshop comparison and browser acceptance before adoption. |
| Small format change | Lossless unsigned 16-bit FITS for representable samples | Removes demonstrated Siril per-frame rescaling and halves ordinary 16-bit payload size. Preserve signed 32-bit fallback and test ASTAP because it uses the same encoder. |
| Next-night operational preparation | Actual-filter autofocus and a visually inspected test frame; explain image metrics honestly | NINA solved tonight's focus problem. No evidence requires implementing autofocus before the next observation. |
| Next-night processing discipline | Explicit selected-image manifest, resolved pairwise image checks, provisional-color labels | Seven objectively spoiled exposures required exclusion; original images remain useful evidence. Keep processing external and reproducible. |
| Defer unless separately chosen | Built-in autofocus, automatic frame rejection/pause, SPCC integration, calibration/stacking suite, durable sequences | Larger capabilities and unresolved physical causes would otherwise crowd out reliable acquisition. |

## 1. Preview tint: fix one renderer and its retained derivatives

### Verified current path

- [preview.ts:50](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/preview.ts#L50) samples all Bayer phases but chooses **one** raw-sample 1% black point and 99.9% ceiling. [preview.ts:66](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/preview.ts#L66) bilinearly reconstructs RGB and applies the same transfer without channel-background correction.
- [display-stretch.ts:4](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/display-stretch.ts#L4) implements the existing asinh curve, strength 10, and exact quarter-step lookup optimization. Small raw background differences can become a pronounced display cast; this is not evidence of a Bayer swap.
- [preview.ts:15](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/preview.ts#L15) derives native and fitted PNGs from the same displayed pixels; [preview.ts:27](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/preview.ts#L27) averages displayed pixels for the fit image.
- [capture/controller.ts:97](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/capture/controller.ts#L97) computes preview, raw-data statistics and FITS independently from the same acquisition. Preserve that separation. Alignment also consumes `previewPng`, through [alignment/controller.ts:39](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/alignment/controller.ts#L39).
- Live/Capture and Observe both use the image-loader primitive: [LatestImage.tsx:16](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/capture/LatestImage.tsx#L16), [CaptureHub.tsx:10](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/capture/CaptureHub.tsx#L10). Saved detail uses the same viewer at [saved-images.tsx:67](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/routes/saved-images.tsx#L67); collection cards choose fitted/native at [saved-images.tsx:77](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/routes/saved-images.tsx#L77).

### Smallest implementation plan

1. Use retained real exposures as the design corpus: ordinary star field, pre/post-focus discs, warm/cooled L-Ultimate Veil, a dark/low-signal frame, and a trailed frame. Keep FITS immutable. In the workshop compare the current renderer with a bounded display-only channel-background neutralization followed by one linked stretch. Tune robustness and stretch with actual images, rather than imposing a gray-world balance on an emission-dominated nebula. Do not claim stellar/nebular colors are scientifically calibrated.
2. Put the accepted display transform at the existing imaging boundary. Keep one transform for native and fitted views; keep raw statistics, solving and exported samples unchanged. Preserve the yielding/compression behavior and avoid allocating several full 26 MP RGB float arrays.
3. Keep blank/invalid-background fallback explicit. Mono behavior and border/Bayer phase handling must remain correct. The exact estimator is a prototype result to evaluate, not a predetermined claim that per-channel min/max stretching is correct.

### Already-retained artifact strategy

Changing `preview.ts` alone leaves every retained PNG unchanged:

- [saved-images/store.ts:11](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/saved-images/store.ts#L11) stores `original.fits`, `preview.png`, `fit.png`; saves are idempotent at [store.ts:103](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/saved-images/store.ts#L103).
- [store.ts:175](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/saved-images/store.ts#L175) validates completed records/files and reconstructs URLs; it does not regenerate images.
- Both retained and live preview routes advertise immutable caching: [saved-images/routes.ts:64](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/saved-images/routes.ts#L64), [capture/routes.ts:178](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/capture/routes.ts#L178). Browser validation requires exact current URLs at [capture/validation.ts:65](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/capture/validation.ts#L65) and [validation.ts:85](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/capture/validation.ts#L85).

Recommend preserving the originally retained FITS **and original preview bytes**, adding only an explicit versioned derived-preview pair and a small atomic pointer/descriptor. Default browsing should use the accepted current treatment; renderer-version URLs prevent old immutable cache entries from impersonating the new rendering. A bounded maintenance operation should regenerate retained derivatives before the next session, with staged writes, verification, idempotent reruns and no incomplete pair becoming visible. Do not perform expensive whole-archive regeneration on every GET or broaden this into an image-processing database.

This needs a bounded reader at the FITS/artifact boundary for the Vela-produced formats (existing signed 32-bit and new unsigned 16-bit); do not embed ad-hoc FITS decoding in HTTP handlers or the browser. Reject malformed/unsupported files explicitly, and leave their legacy preview available with honest status rather than silently claiming migration succeeded.

**Material choice:** the current UI promises “the preview you inspected” and download-preview returns those exact stored bytes ([saved-images.tsx:76](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/routes/saved-images.tsx#L76), [saved-images/routes.ts:37](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/saved-images/routes.ts#L37)). Recommend the default displayed/downloaded preview become the refreshed treatment, while the original PNG remains physically preserved as provenance. Confirm this behavior and revise the promise; do not silently change archival semantics or invent a full version-history UI.

### Gates

- Numerical tests: differing Bayer-phase background offsets, true red/blue source signals, gradients, starless scenes, clipped/invalid inputs, all four Bayer phases and borders, mono, and bitwise unchanged acquisition samples. Existing tests are at [preview.test.ts:19](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/preview.test.ts#L19) and [preview.test.ts:42](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/preview.test.ts#L42). Existing exact-byte assertions encode the old display behavior; update only where the approved treatment actually changes it.
- Storage/route tests: interrupted derivative write, concurrent refresh/read, legacy record, no native/fit version mismatch, unchanged FITS hash, unchanged acquisition metadata/statistics, version-specific cache URLs, and unavailable refresh fallback.
- Workshop: real images at fit/native and thumbnail scales, desktop/phone, Vela Current/dark/density1. Compare with a trusted FITS viewer using comparable display choices; a different automatic stretch is not ground truth for color.
- Independent verifier **OK**, then Chris's browser acceptance of Observe/Capture fit+100%/saved detail/collection/download and at least one existing retained image. Existing browser tests cover loaded-image identity and fit/native switching at [capture.e2e.ts:63](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/tests/capture.e2e.ts#L63) and saved browsing at [capture.e2e.ts:347](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/tests/capture.e2e.ts#L347).
- No live hardware needed to validate this change; replay retained originals. A later live example is acceptance evidence, not a reason to run device experiments during implementation.

## 2. FITS unsigned 16-bit compatibility, preserving all sample values

### Evidence and exact owner

Tonight's original signed 32-bit ADU16 files triggered Siril per-image rescaling; unsigned 16-bit working copies fixed it. The audit proved complete-array equality for all three cooled smoke-test inputs and correct Bayer-channel assignment: `data/observing-20260914/audit/processing-audit.md`.

[imaging/fits.ts:23](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/fits.ts#L23) always emits BITPIX=32; [fits.ts:47](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/fits.ts#L47) validates signed 32-bit integer range and [fits.ts:51](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/fits.ts#L51) writes big-endian signed values. Metadata already records TOP-DOWN and origin-adjusted Bayer pattern at [fits.ts:26](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/fits.ts#L26) and [fits.ts:35](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/fits.ts#L35). Acquisition Bayer-origin normalization is correctly owned at [alpaca/acquisition.ts:201](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/acquisition.ts#L201).

**Important coupling:** [plate-solving/solver.ts:68](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/plate-solving/solver.ts#L68) also uses `encodeCaptureFits`; this is not only a saved-download change.

### Smallest plan and gates

- Validate all samples, select unsigned 16-bit only when every sample is an integer in 0–65535, encode BITPIX=16 plus BZERO=32768/BSCALE=1 and signed storage `value−32768`. Otherwise retain the existing signed 32-bit representation, including negative and >65535 values. Never clamp, scale min/max, reverse rows, infer bit depth from camera brand, or mutate the samples. Numeric representability is sufficient for lossless storage; it is not a claim about physical sensor bit depth.
- New captures/solver temporaries can use the improved encoder; already-retained original files remain untouched. If an old artifact needs Siril compatibility, produce an explicitly derived export/copy, not an in-place rewrite of `original.fits`.
- Tests: 0/32767/32768/65535, negative and 65536 fallback, signed extremes, NaN/fraction rejection, FITS header/data padding and endianness, DATE-OBS/TIMESRC/BAYERPAT/ROWORDER preservation, cooperative yielding. Keep [fits.test.ts:11](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/fits.test.ts#L11) signed-range protections and [fits.test.ts:28](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/fits.test.ts#L28) Bayer-origin tests.
- Independently decode resulting fixtures with Astropy or CFITSIO; verify all values, not just min/max. Replay several retained real frames through Siril: no unsuitable-for-sequence scaling warning, same ADU data, successful registration. Replay a retained solved exposure through ASTAP and compare solution against existing sky landmarks, allowing numerical solver tolerance rather than byte equality.
- Focused encoder/capture/store/solver tests, relevant builds/lint, independent verification. No new visual design is required. Because FITS download behavior is user-facing, prepare a concrete download/import review and obtain Chris's acceptance under the default repository policy; no new camera exposure is needed.

## 3. Cooling: existing telemetry, missing setpoint/control/context

### Verified current capability

- [alpaca/provider.ts:452](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider.ts#L452) already reads camera state and actual CCD temperature; [provider.ts:455](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider.ts#L455) reads setpoint/power capabilities; [provider.ts:467](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider.ts#L467) reads actual coolerOn; [provider.ts:479](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider.ts#L479) reads/validates power.
- It **does not read the requested setpoint**. Its normalized cooling shape at [alpaca/model.ts:58](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/model.ts#L58) has on/off/capabilities/power but no temperature target.
- [server/web/rig-detail.ts:119](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/web/rig-detail.ts#L119) intentionally projects only useful cooling, omitting capability flags; [model/rig-detail.ts:68](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/model/src/web/rig-detail.ts#L68) has on/off/power only.
- Rig details already render Sensor/Cooler/Power at [RigDeviceCard.tsx:138](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/rig-detail/RigDeviceCard.tsx#L138). Capture merely links to those details at [capture.tsx:76](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/routes/capture.tsx#L76); [CaptureView:55](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/model/src/web/capture.ts#L55) carries only camera name.
- No cooling write capability is exported by [alpaca/index.ts:18](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/index.ts#L18). Camera selection is a durable configuration operation, not a thermal command: [rig/imaging-camera.ts:49](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/rig/imaging-camera.ts#L49).

### Smallest plan

1. Extend the existing camera inspection contract with actual requested temperature where supported; preserve unsupported versus failed versus stale/partial distinctions. Do not infer coolerOn from measured temperature or from a successfully entered setpoint.
2. Add one narrow thermal-control adapter capability resolving the selected camera's stable ID and current name, validating connection/capabilities, issuing each requested write once, then confirming its own property by readback. Keep confirmed on/off, confirmed requested target, actual temperature and pending/unconfirmed command distinct. A lost response is reconciled by reads, never replayed. Do not wait for the sensor to reach target before reporting that the target setter was confirmed.
3. Project the selected camera's thermal observation near Capture preparation, reusing existing normalized status instead of duplicate device-property logic in React. Workshop one calm control treatment: on/off, target, actual temperature, optional power, and honest warming/cooling/unknown indications. No auto-on, no mandated 5°C default, no “safe/ready” badge inferred from closeness, and no temperature-based capture blockade.
4. Preserve requested target and actual cooler status after a mini-PC restart as **observations**. Never automatically restore a previously intended physical state without an explicit separate product decision.

**Material lease decision:** [rig/operations.ts:5](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/rig/operations.ts#L5) permits one Rig command owner; capture holds it for the entire run through [capture/routes.ts:121](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/capture/routes.ts#L121) and [routes.ts:145](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/capture/routes.ts#L145). The smallest first release offers thermal writes while capture is idle and observes cooling throughout a run. Confirm whether Chris requires changing cooling during active repeated capture. If yes, design explicit compatible command ownership; do not quietly bypass the current lease or introduce a general command scheduler.

### Gates and unknowns

- Verify ASCOM requirements for `CoolerOn`, `SetCCDTemperature`, capability relationships, unsupported properties, writable domain and cancellation before coding; existing fixtures include open-loop/no-cooler/partial cases at [provider-inspection.test.ts:212](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider-inspection.test.ts#L212), [test.ts:283](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider-inspection.test.ts#L283) and [test.ts:376](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider-inspection.test.ts#L376).
- Deterministic tests: wrong/replaced camera, unsupported/open-loop control, finite/invalid target, conflicting owner, partial read failure, off-at-near-setpoint, successful setter then thermal lag, lost write response/readback match, unresolved write, restart with actual cooler off. Thermal read failure must not imply the camera cannot expose if existing acquisition preconditions still hold.
- Workshop and browser: supported/unsupported, off near target, active cooldown, changed target pending, confirmation unknown, disconnected last-known values, optional power absent, phone/desktop. Independent **OK**, then Chris's implemented-browser acceptance.
- Controlled hardware acceptance later, with starting state recorded: query capabilities, command the agreed target/on/off once, confirm state and measured response, restore agreed final state. This planning audit authorizes no such action now. The local simulator currently reports no cooler-power capability ([service.ts:186](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/rig-simulator/src/service.ts#L186)); do not mistake that fixture for cooled-hardware validation.

## 4. Autofocus and preparation context: no new autofocus subsystem before the next night

Tonight demonstrated separate problems: guiding removed long drift trails; NINA autofocus with the imaging filter materially improved star profiles. Later small HFR changes did not uniquely diagnose focus. Actual focuser position remained 32842 while profile behavior and guide losses changed.

Current capture readiness checks camera identity, connection, Rig ownership and idle activity, not optical quality: [capture/routes.ts:72](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/capture/routes.ts#L72), [routes.ts:92](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/capture/routes.ts#L92). Keep that truthful operational meaning. [alpaca/provider.ts:617](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider.ts#L617) reads focuser capability/position and [provider.ts:643](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/alpaca/src/provider.ts#L643) moving/temperature only; [RigDeviceCard.tsx:161](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/rig-detail/RigDeviceCard.tsx#L161) displays those observations. There is no implemented autofocus operation in the inspected capability exports.

For next night, document the short actual workflow: install the intended imaging filter, complete autofocus in the chosen tool, ensure that tool has released camera ownership, enable/confirm cooling, take an appropriately exposed frame, inspect central and outer stars at native scale, then choose repeated exposure settings. This is an operator aid, not a mandatory wizard or a certificate that focus is optimal.

If readiness context is added alongside cooling, phrase unknown focus as unknown; external autofocus history is not observable from an idle focuser position. Do not invent a confirmed “focused” state or invalidate it by an arbitrary timer.

Defer built-in autofocus until Chris chooses it as a separate workstream. The material unknowns are actual focuser selection, movement/backlash behavior, bounded travel and cancellation cleanup, filter-specific exposure/star sampling, and what constitutes accepted best focus. Exploration should establish a repeatable single-filter focus trial before proposing filters, schedules, temperature compensation or recovery engines. Such a future operation needs its own workshop, adapter tests, fresh verifier and controlled hardware acceptance.

## 5. Image-quality metrics and selection: expose limits, avoid false certainty

The current implementation already states its scientific meaning correctly:

- [statistics.ts:59](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/statistics.ts#L59) detects noise-relative peaks; [statistics.ts:105](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/statistics.ts#L105) includes only accepted measured stars in the final median; [statistics.ts:114](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/statistics.ts#L114) returns that population's count.
- [statistics.ts:225](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/statistics.ts#L225) rejects low signal, clipping/unconverged outer flux and strongly elongated profiles. Thus a severely bad frame can retain a deceptively small-HFR survivor population. This is not necessarily a numerical HFR bug.
- [CaptureImageStatistics:1](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/model/src/web/capture.ts#L1) contains only count and median HFR. [LatestImage.tsx:193](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/capture/LatestImage.tsx#L193) labels these “Stars” and “HFR · px” with title-only explanation; zero/unavailable are already distinct at [LatestImage.tsx:197](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/capture/LatestImage.tsx#L197).

Smallest next-night product improvement: make “measured stars” and HFR's scope understandable in progressive detail accessible on phone, retaining zero versus unavailable and loaded-image/metadata atomicity. Do not add a “good focus” indicator, score, automatic rejection, or stop policy based on count/HFR thresholds.

Keep a bounded offline selection workflow for now: preserve every original; compare a questionable frame against the nearest good reference using resolved stars, broad displacement matching after guide recovery, pairwise samples rather than a whole-night intersection, major/minor shape and actual native profile inspection. Long plumes can escape core-only FWHM; aperture flux loss can mean displaced light rather than clouds. The audit caught invalid registration onto fixed sensor artifacts and replaced those results; that check must be explicit in any reusable tool. Do not port the night's ad-hoc scripts wholesale into capture's critical path.

Selection remains a processing fact attached to image identity and a specific manifest, not deletion or an automatically inferred durable capture run. The existing `data/observing-20260914/make_batch.py:6` snapshots eligible originals and exclusion IDs; `data/observing-20260914/make_batch.py:19` records selected sources. A later image-exclusion UI can be small artifact metadata plus an export manifest if explicitly requested; do not introduce it merely to reproduce tonight's external workflow.

Tests for copy/visibility changes should extend existing starless/unavailable browser cases ([capture.e2e.ts:301](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/tests/capture.e2e.ts#L301)) and loading identity cases. If numerical analysis changes later, preserve independent integrated-profile tests at [statistics.test.ts:49](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/statistics.test.ts#L49) and shape-rejection cases at [test.ts:143](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/server/src/imaging/statistics.test.ts#L143); add deliberate changing-population and motion-profile examples without asserting that a shrinking median means improvement. Workshop/independent/browser gates apply to new presentation. Offline retained images suffice; no immediate hardware experiment is required.

## 6. Provisional color and downstream processing boundary

Siril command completion is not trustworthy evidence of successful color calibration. Tonight's SPCC fits repeatedly warned of imprecision; simple gradient removal did not help, and the bright-star subset only partly improved residuals. Valid catalog astrometry and acceptable registration do not establish calibrated color.

The current session tool now names the result an SPCC attempt and supports skipping it: `data/observing-20260914/make_batch.py:33`, `data/observing-20260914/make_batch.py:41`. Current app previews are already display aids ([LatestImage.tsx:202](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/apps/web/src/features/capture/LatestImage.tsx#L202)); there is no reason to integrate SPCC to fix preview tint.

For the next session, preserve raw inputs, selected-frame manifest, unstretched linear stack, processing steps and explicit provisional-color label. A catalog-oriented output must be verified against an actual sky landmark, not only its own output-WCS assertion; the existing 52 Cyg audit establishes this for the saved stacks (`data/observing-20260914/audit/processing-audit.md`). No extra ROWORDER flip is warranted.

Defer SPCC/stacking/calibration implementation inside Vela. Further external color experiments should first address concrete photometric limits—matching calibration frames if available, sufficiently strong unsaturated isolated stars, blends/large stellar footprints and narrowband fit conditioning—rather than repeatedly rerunning a warning-producing fit or treating natural-looking color as physical truth. Filter identity, calibration-frame availability and a trustworthy color reference remain specific unknowns. None prevents a useful explicitly provisional acquisition preview.

## Delivery and verification summary

Planning has run no tests/builds because no source changed. Before implementation, choose the unresolved preview-download semantics and cooling-during-capture behavior; all other listed technical choices can be settled by focused prototypes and evidence without reopening already explicit preferences.

Use focused Vitest files from the root (`pnpm exec vitest run …`), relevant package builds and lint per [CODING_STANDARDS:152](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/CODING_STANDARDS.md#L152). The cooling and preview-version slices span model/adapter/server/web and may warrant full `pnpm check`; do not run it by habit for documentation-only changes. Capture browser checks use `pnpm --filter @vela/web test:browser tests/capture.e2e.ts` with isolated fixtures.

Workshop design authority and the independent-OK-then-Chris-browser-review sequence remain explicit at [workshop operations:7](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/docs/component-workshop-operations.md#L7). Extend the existing capture/saved-image specimens at [Panel.capture-runs.specimen.tsx:131](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/ui/src/drafts/Panel.capture-runs.specimen.tsx#L131) and [Panel.saved-images.specimen.tsx:181](https://github.com/chrhicks/vela/blob/34a62367f605212d5b317d163109e6047388d2e0/packages/ui/src/drafts/Panel.saved-images.specimen.tsx#L181), using real retained fixtures for appearance. Update the owning imaging, saved-images, capture, model and Alpaca READMEs as each accepted boundary changes; this report should remain a dated investigation rather than become competing durable architecture.
